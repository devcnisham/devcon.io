# Parked

Things noticed while doing something else, written down instead of done.

**This is an inbox, not a seventh document about v2's state.** It does not
restate what is built, what passes or what is left — `SHIP.md` decides,
`v2/task.md` tracks, and adding another describer would worsen task 20. Nothing
here is started, scheduled, or promised. Delete an entry when it is done or
when it stops being worth doing.

Newest first. Each entry says what was noticed, why it was left, and what it
would cost.

---

**Empty.** The three entries this file opened with on 2026-08-11 were all done
the same day, on request, and deleted from here rather than marked done — that
is what the rule above says to do, and a list of completed items is the kind of
restatement this file exists not to become. `CHANGELOG.md` has them.

For the record, so the emptiness is not mistaken for the file having never been
used: `GIT_CONFIG_NOSYSTEM` got a test with a control, two dead sandbox allows
were removed and replaced with a test proving the narrowing is real, and
`"type": "module"` landed after a before-and-after build diff.
